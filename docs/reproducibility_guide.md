# Reproducibility guide

No LLM API credential is required to validate included outputs or regenerate public descriptive tables. Remote model re-execution is not expected to yield token-identical outputs because hosted services can change. The reproducibility claim is therefore based on frozen inputs, archived outputs, validation audits, analysis-ready tables, and manifests.
