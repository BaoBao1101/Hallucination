# Neutral paired evaluation

The public strict neutral paired inference includes only ChatGPT v1.
Gemini neutral v1, v2, and v3 are archived under `Excluded/` as audit artifacts and are not used in main neutral inference.
